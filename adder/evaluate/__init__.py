"""Checking that a lever is real before anyone trusts it.

If `measure` says what happened and `decide` says what to do, this package asks
whether the advice held up: replay, A/B, calibration, and the claim tests that
the README's headline numbers are re-run against.
"""
